import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <header>
      <div class="container">
        <a href="/" title="qwik">
          LOGO
        </a>
      </div>
    </header>
  );
});
